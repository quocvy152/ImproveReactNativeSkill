export type RenderPropType = {
  children: React.ReactNode;
  isVisible: boolean;
  setVisible: any;
  typePopup: string;
};

export type HandlePopupType = {
  isVisible: boolean;
  setIsVisible: any;
};
