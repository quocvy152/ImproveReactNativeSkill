export type UseReducerDemoType = {
  action: string;
  state: number;
};

export type UseReducerGetUsersResType = {
  error: boolean;
  data: [];
  isLoading: boolean;
};

export type UseReducerGetUsersActionType = {
  type: string;
  data: [];
};
